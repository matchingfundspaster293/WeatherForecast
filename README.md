# WeatherForecast: End-to-End Проект по Прогнозированию Временных Рядов

Этот проект представляет собой полный **End-to-End (E2E)** пайплайн для сбора, хранения, анализа и прогнозирования погодных данных. Основная цель — сравнить эффективность трех различных подходов к прогнозированию временных рядов на реальных данных: классического статистического метода (ARIMA), современного SOTA-решения (N-BEATS) и кастомной нейросети (LSTM), написанной с нуля на PyTorch.

## 🚀 Ключевые Особенности

*   **Промышленно-надежный скрапер:** Разработан кастомный, параллельный веб-скрапер на `requests` и `ThreadPoolExecutor` для сбора многолетнего архива погоды. Скрапер включает в себя:
    *   Автоматическую генерацию URL для всего временного диапазона.
    *   Механизм повторных попыток (`retries`) с экспоненциальной задержкой (`exponential backoff`) для обработки сетевых ошибок.
    *   Извлечение данных из встроенных JS-объектов для максимальной надежности.
*   **Многомерное прогнозирование:** Модели предсказывают не одно среднее значение, а вектор из трех показателей на следующий день: температура утром, днем и вечером.
*   **Турнир моделей:** Реализовано и сравнено три подхода к прогнозированию:
    1.  **Baseline:** Классический `ARIMA`.
    2.  **SOTA "из коробки":** `N-BEATS` из библиотеки Darts.
    3.  **Собственное решение:** Кастомная `LSTM` на PyTorch.
*   **Полный E2E-пайплайн:** Проект охватывает все этапы от сбора данных до анализа результатов, включая хранение в SQLite, разведочный анализ (EDA) и Feature Engineering.

## 🛠️ Используемые технологии

| Область | Технологии |
| :--- | :--- |
| **Сбор данных** | `Python`, `requests`, `BeautifulSoup4` |
| **Хранение данных** | `SQLite`, `SQLAlchemy` |
| **Обработка и анализ** | `Pandas`, `NumPy`, `Matplotlib`, `Seaborn`, `statsmodels` |
| **Машинное обучение** | `PyTorch`, `Darts`, `Scikit-learn` |
| **Структура проекта** | Модульный подход (`src/`), Jupyter Notebooks |

## 📁 Структура проекта

```
weather_vault/
│
├── .gitignore
├── README.md
├── requirements.txt
│
├── data/
│   └── (здесь будет создана weather.db)
│
├── notebooks/
│   ├── 1_data_exploration.ipynb  # Разведочный анализ данных (EDA)
│   └── 2_model_training.ipynb    # Обучение и сравнение моделей
│
└── src/
    ├── __init__.py
    ├── config.py                 # Файл конфигурации
    ├── database.py               # Модуль для работы с БД
    ├── scraper.py                # Основной модуль скрапера
    └── run_scraper.py            # Скрипт для запуска скрапера
```

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone [URL вашего репозитория]
cd weather_vault
```

### 2. Создание и активация виртуального окружения

```bash
# Для Windows
python -m venv venv
venv\Scripts\activate

# Для macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск скрапера для сбора данных

Этот шаг необходимо выполнить первым. Скрипт соберет весь архив погоды и сохранит его в локальную базу данных `data/weather.db`.

**Внимание:** Процесс может занять продолжительное время (20-40 минут), так как он включает "вежливые" задержки, чтобы не перегружать сервер-источник.

```bash
python -m src.run_scraper
```

### 5. Проведение анализа и обучения

После успешного сбора данных можно запускать Jupyter Notebooks.

```bash
jupyter notebook
```

В открывшемся окне браузера перейдите в папку `notebooks/` и последовательно откройте:
1.  `1_data_exploration.ipynb` — для изучения и визуализации собранных данных.
2.  `2_model_training.ipynb` — для обучения, сравнения и оценки моделей прогнозирования.

## 🕷️ Глубокий взгляд на скрапер

Модуль сбора данных является сердцем этого проекта и демонстрирует решение реальных инженерных задач.

### Задача

Источник данных не предоставляет публичного API. Необходимо было разработать решение для сбора многолетнего архива погоды напрямую с веб-страниц сайта.

### Эволюция решения

1.  **Первоначальный анализ** показал, что все необходимые числовые данные (температура, давление, скорость и направление ветра, облачность) находятся не в HTML-тегах, а встроены в JavaScript-объект `mart.forecastMap` на каждой странице. Это позволило отказаться от хрупкого парсинга HTML-тегов в пользу надежного извлечения данных из JS-кода.
2.  **Архитектура:** Был выбран **проактивный подход** вместо реактивного. Вместо того чтобы переходить по ссылкам "предыдущий день", скрапер **генерирует полный список URL** для всего целевого диапазона дат.
3.  **Параллелизм:** Такой подход позволил реализовать **параллельный скрапинг** с использованием `concurrent.futures.ThreadPoolExecutor`. Это значительно (в `MAX_WORKERS` раз) сокращает общее время сбора данных.
4.  **Надежность:** При массовых запросах сервер-источник может выдавать ошибки (`502 Bad Gateway` и др.). Для борьбы с этим в скрапер встроен механизм **повторных попыток с экспоненциальной задержкой**, который позволяет автоматически "пережидать" временные сбои на сервере.

### Этические соображения

Скрапер разработан с учетом уважительного отношения к ресурсу:
*   В конфигурации установлены задержки между запросами.
*   Проект строго соответствует пункту **"личное некоммерческое использование"** из пользовательского соглашения сайта-источника.
*   Собранные данные **не распространяются** в данном репозитории (папка `data/` включена в `.gitignore`).

---

## ⚖️ Лицензия и Дисклеймер

Данный проект является исключительно учебным и демонстрационным. Он не предназначен для коммерческого использования. Весь код распространяется под лицензией MIT.