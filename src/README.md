## 📐 Архитектура

Код разделен на несколько модулей, каждый из которых отвечает за свою конкретную задачу:

*   **`config.py`**: Файл конфигурации. Содержит все "магические числа" и настройки: URL-адреса, пути к файлам, параметры скрапера (`MAX_WORKERS`, `REQUEST_DELAY`), диапазоны дат и т.д. Позволяет легко изменять поведение приложения, не трогая основной код.

*   **`scraper.py`**: Основной модуль для сбора данных. Включает в себя всю логику веб-скрапинга:
    *   `generate_url_list()`: Создает полный список URL для заданного диапазона дат.
    *   `parse_data_from_js()`: "Сердце" парсера. Извлекает все необходимые погодные метрики напрямую из встроенного JS-объекта на странице, что обеспечивает максимальную надежность.
    *   `scrape_url()`: Обёртка для одного URL, реализующая механизм повторных попыток с экспоненциальной задержкой.
    *   `run_parallel_scrape()`: Оркестратор, который запускает `scrape_url` в несколько потоков с помощью `ThreadPoolExecutor`.
    *   `aggregate_data()`: Модуль постобработки. Преобразует "сырые" 3-часовые данные в агрегированный "широкий" формат (`одна строка = один день`), вычисляя средние значения для каждого периода (утро, день, вечер).

*   **`database.py`**: Простой модуль для инкапсуляции всей логики работы с базой данных. Использует `SQLAlchemy` для создания `engine` и `pandas.to_sql` для сохранения данных в `SQLite`.

*   **`run_scraper.py`**: Исполняемый скрипт. Является точкой входа для запуска всего E2E-пайплайна сбора и обработки данных. Он последовательно вызывает функции из других модулей, оркеструя весь процесс.

## 🚀 Принципы проектирования

*   **Разделение ответственности (Separation of Concerns):** Каждый модуль делает только одну вещь, но делает ее хорошо.
*   **Конфигурация вне кода:** Все изменяемые параметры вынесены в `config.py`.
*   **Отказоустойчивость:** Скрапер спроектирован так, чтобы самостоятельно справляться с временными сетевыми ошибками.
*   **Производительность:** Использование параллелизма (`ThreadPoolExecutor`) позволяет значительно ускорить процесс сбора данных.
*   **Надежность:** Выбор в пользу парсинга JS-объекта вместо хрупких HTML-тегов обеспечивает стабильность скрапера в долгосрочной перспективе, даже если верстка сайта изменится.

## ⚖️ Правовые и этические аспекты

Данный проект разработан с полным осознанием и уважением к правилам использования данных, установленным сайтом-источником.

### Анализ Пользовательского соглашения

Перед началом сбора данных было тщательно изучено Пользовательское соглашение сайта-источника. Ключевые выводы:
*   **Разрешено:** "Данные предназначены исключительно для **личного некоммерческого использования**". Проект полностью соответствует этому требованию, так как является учебным и не преследует коммерческих целей.
*   **Запрещено:** "...любое **копирование, воспроизведение, ... размещение в свободном доступе (опубликование) ... и/или в коммерческих целях** без предварительного письменного разрешения...".

### Принципы, реализованные в проекте

На основании этого анализа, в проекте строго соблюдаются следующие принципы:

1.  **Некоммерческое использование:** Проект является исключительно образовательным и портфолийным.
2.  **Приватность данных:** Собранные данные (`data/weather.db`) **не публикуются** и **не распространяются**. БД `weather.db` намеренно включена в `.gitignore`, чтобы исключить случайную выгрузку данных в публичный репозиторий.
3.  **"Вежливый" скрапинг:** Скрапер спроектирован для минимизации нагрузки на сервер-источник. В `src/config.py` установлены консервативные значения для количества параллельных потоков (`MAX_WORKERS`) и задержек между запросами (`REQUEST_DELAY`).
4.  **Образовательная цель:** Цель данного репозитория — продемонстрировать инженерные и аналитические навыки, а не предоставить инструмент для массового скачивания или распространения чужих данных.

---
### Лицензия

Код данного проекта распространяется под лицензией MIT.